// Q17 Yes, if we add another new command, we still need to change the Game class
/*Command Implementation: We also need to add a new method to the Game class that 
implements the functionality of the new command (like how goRoom(), look(), eat(), 
etc.are implemented). 
 * 
 * 
 */ 